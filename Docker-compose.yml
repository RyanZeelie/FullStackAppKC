version: '3.8'

services:
  reverse-proxy:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - api
      - frontend

  api:
    build:
      context: ./API/CMApi
      dockerfile: Dockerfile

  frontend:
    build:
      context: ./Client
      dockerfile: Dockerfile
      
networks:
  your_custom_network:
    name: your_custom_network
    external: true